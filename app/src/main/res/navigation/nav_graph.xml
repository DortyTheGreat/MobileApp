<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">
    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.cardriver.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/noEthernet"
        android:name="com.example.cardriver.NoEthernet"
        android:label="activity_no_ethernet"
        tools:layout="@layout/activity_no_ethernet" />
    <activity
        android:id="@+id/onboarding"
        android:name="com.example.cardriver.Onboarding"
        android:label="activity_onboarding"
        tools:layout="@layout/activity_onboarding" />
</navigation>